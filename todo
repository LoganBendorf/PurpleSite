
Add button to expand recevied emails

Fix form ui

Clear text after sending message, popup if not sent.

Now that functions have been moved out into their own files.
    Create tests for them
    Multithreading

Make emoji buttons nice

Use some kinda of arena memory allocation instead of mallocing, so it's easier to debug, you can just read the memory like an array

Checkbox link for upvotes
    https://moderncss.dev/pure-css-custom-checkbox-style/

Client side input validation.
    https://www.theodinproject.com/lessons/node-path-intermediate-html-and-css-form-validation

Compression
    https://app.codecrafters.io/courses/http-server/overview

Robots.txt?

Add button on mobile that switches between email and compose, cause the screen width is too small.
    Like [Emails Compose] and they'll have different background colors, and when you switch
    between them it will darken the one that is inactive. Make it slide or something too.